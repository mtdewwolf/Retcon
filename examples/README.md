# Examples

Small example projects used for manual testing of Retcon workflows — e.g. a tiny web app
with a dev server for browser verification, or a repo with failing tests for the
"fix failing test" flow. Populated as the corresponding features land.
