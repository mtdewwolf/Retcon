name: retcon_diff_viewer
description: "Retcon diff review widgets with unified and side-by-side views."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  retcon_design_system:
    path: ../retcon-design-system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
