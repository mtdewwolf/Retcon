name: retcon_terminal_view
description: "Retcon terminal renderer and panel widgets wired to retcon-core terminal RPC."
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  retcon_design_system:
    path: ../retcon-design-system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
