# retcon_terminal_view

Flutter terminal renderer for Retcon. Created in Phase 14 (terminal system); kept as a
separate package so the renderer stays replaceable (Flutter terminal rendering
performance is the project's top technical risk).
