{
  "name": "@retcon/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/generate-brand-assets.mjs && astro check && astro build",
    "preview": "astro preview",
    "test": "npm run build && node scripts/verify-build.mjs",
    "check:launch": "node scripts/check-launch-assets.mjs",
    "assets": "node scripts/generate-brand-assets.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3"
  }
}
