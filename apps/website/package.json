{
  "name": "@retcon/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "test": "npm run build && node scripts/verify-build.mjs",
    "check:launch": "node scripts/check-launch-assets.mjs",
    "assets": "node scripts/generate-brand-assets.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.5",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3"
  }
}
