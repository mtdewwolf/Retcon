name: retcon_test_dashboard
description: "Visual dashboard for Retcon's multi-stack test suite — discover, run, and focus on what needs attention."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  retcon_design_system:
    path: ../../packages/retcon-design-system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
