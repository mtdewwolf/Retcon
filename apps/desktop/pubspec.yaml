name: retcon_desktop
description: "The Retcon desktop shell: supervise and verify AI coding agents."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  retcon_design_system:
    path: ../../packages/retcon-design-system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
