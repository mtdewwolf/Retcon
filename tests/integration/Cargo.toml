[package]
name = "retcon-integration"
description = "Cross-cutting integration harness for Retcon core RPC flows"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
retcon-core = { path = "../../crates/retcon-core" }
retcon-platform = { path = "../../crates/retcon-platform" }
retcon-protocol = { path = "../../crates/retcon-protocol" }
serde_json = { workspace = true }
tempfile = "3"
tokio = { workspace = true }

[dev-dependencies]
serial_test = "3"

[lints]
workspace = true
