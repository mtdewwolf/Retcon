# Test fixtures

Cross-component test fixtures shared by Rust, Dart, and TypeScript tests — e.g. recorded
protocol messages, malformed-message cases, and sample provider output. Fixtures are
versioned alongside the protocol schemas in `schemas/`.
